workflow = "envol.yml"
publish_workflow = "envol-publish.yml"
publishers = ["github", "crates"]
version_files = ["Cargo.toml", "Cargo.lock", "package.json", "package-lock.json"]
required_artifacts = ["envol-linux-x64.tar.gz"]
auto_promote = false

[lines.stable]
branch = "main"
channel = "stable"

# Enable after creating the branch:
# [lines.next]
# branch = "next"
# channel = "alpha"
